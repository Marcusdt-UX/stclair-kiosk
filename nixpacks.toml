# Disable Caddy — proxy-server.js handles HTTP + WebSocket directly
[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm install && npm run build"]

[start]
cmd = "node proxy-server.js"
